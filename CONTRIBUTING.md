# 새 스킬 추가 가이드

Glen's Claude Skills에 새로운 스킬을 추가하는 방법을 설명합니다.

---

## 🎯 Claude Code 표준 구조

모든 스킬은 **Claude Code 표준**에 따라 `.claude/skills/` 폴더에 위치합니다:

```
.claude/skills/[skill-name]/
├── SKILL.md              ← 필수: 메인 스킬 파일
├── INSTALL.md            ← 선택: 설치 가이드
├── reference.md          ← 선택: 상세 참고
├── examples.md           ← 선택: 코드 예제
├── [tool-folders]/       ← 선택: 핵심 도구/라이브러리
└── ...
```

**핵심 원칙:**
- 스킬은 **자체 포함**(self-contained)되어야 함
- 심링크 사용 금지
- 스킬 폴더만 복사하면 다른 프로젝트에서 바로 사용 가능

---

## 📁 새 스킬 생성

### 1단계: 스킬 폴더 생성

```bash
mkdir -p .claude/skills/[skill-name]
cd .claude/skills/[skill-name]
```

**명명 규칙:**
- 소문자 사용
- 하이픈으로 단어 구분
- 짧고 명확하게
  - ✅ `pdf`, `image`, `data-viz`
  - ❌ `PDF`, `img_processing`, `data_visualization`

### 2단계: SKILL.md 작성 (필수)

```markdown
---
name: [skill-name]
description: "스킬 설명"
license: Proprietary
disable-model-invocation: false
user-invocable: true
allowed-tools: Bash, Read, Write, Glob, Grep
---

# [Skill Name]

스킬 설명...

## Quick Start

사용 예제...

## 기능

- 기능 1
- 기능 2
```

### 3단계: 추가 파일 작성 (선택)

필요에 따라 추가:
- `INSTALL.md` - 의존성 설치 가이드
- `reference.md` - 상세 API/사용법 참고
- `examples.md` - 코드 예제 모음
- `install-mac.sh`, `install-linux.sh` - 설치 스크립트

---

## 📝 SKILL.md 작성 가이드

### Front Matter (필수)

```yaml
---
name: skill-name
description: "간단한 설명"
license: Proprietary
disable-model-invocation: false
user-invocable: true
allowed-tools: Bash, Read, Write, Glob, Grep
---
```

### 본문 구조

```markdown
# [Skill Name]

간단한 설명

## Quick Start

빠른 사용 예제

## 기능

주요 기능 설명

## 워크플로우

작업 흐름 설명

## 의존성

필요한 패키지/도구
```

---

## ✅ 체크리스트

### 기본 구조
- [ ] `.claude/skills/[skill-name]/` 폴더 생성
- [ ] `SKILL.md` 작성 (Front Matter 포함)
- [ ] 필요한 추가 파일 작성

### 자체 포함 (Self-Contained)
- [ ] 모든 필요한 라이브러리/도구가 스킬 폴더 내에 있음
- [ ] 심링크 사용 안 함
- [ ] 외부 의존성 최소화

### 문서
- [ ] 사용법 명확히 설명
- [ ] 코드 예제 포함
- [ ] 의존성 설치 방법 포함

### 테스트
- [ ] Claude Code에서 스킬 인식 확인 (`/` 입력 시 표시)
- [ ] 주요 기능 테스트
- [ ] macOS/Linux에서 설치 테스트 (해당 시)

---

## 🌟 모범 사례: PPTX 스킬

[`.claude/skills/pptx/`](.claude/skills/pptx/) 구조 참고:

```
.claude/skills/pptx/
├── SKILL.md              ← 메인 스킬 파일
├── DESIGN.md             ← 디자인 가이드
├── INSTALL.md            ← 설치 가이드
├── build.js              ← 빌드 스크립트
├── styles.css            ← 기본 스타일
├── html2pptx/            ← 핵심 라이브러리 (자체 포함)
├── pptx-skill-tools/     ← 도구 모음 (자체 포함)
├── reference.md
├── examples.md
├── requirements.txt
├── package.json
├── install-mac.sh
└── install-linux.sh
```

---

## 📞 도움말

새 스킬 추가 중 문제가 있으면:

1. **이 가이드** 다시 읽기
2. **[PPTX 스킬](.claude/skills/pptx/)** 참고 (모범 사례)
3. **[docs/](docs/)** 폴더의 다른 가이드 참고

---

**마지막 업데이트**: 2026-01-26

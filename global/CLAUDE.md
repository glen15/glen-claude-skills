# Global Claude Code Guidelines

이 파일은 모든 프로젝트에 적용되는 Glen의 개인 코딩 원칙입니다.

---

## 핵심 개발 철학

### 1. Tidy First (Kent Beck)

**"정리를 먼저, 그 다음 기능"**

```
작업 순서:
1. 🧹 Tidy (정리)    - 코드를 이해하기 쉽게 만들기
2. ✨ Feature (기능)  - 새로운 기능 추가
3. 🧹 Tidy (정리)    - 추가한 코드 정리
```

**절대 규칙**:
- ❌ 정리와 기능을 절대 섞지 말 것
- ❌ 한 커밋에 여러 목적을 담지 말 것
- ✅ 작게 정리하라 (5-15분 단위)
- ✅ 코드가 이해하기 어려우면 먼저 정리

**Boy Scout Rule**:
파일을 열었다면 최소 한 가지는 개선하고 나갈 것

---

### 2. Make it Work → Make it Right → Make it Fast

**이 순서를 반드시 지키며, 절대 건너뛰지 않습니다.**

```
1. 🔧 Make it work (동작하게)
   - 일단 기능이 동작하도록 만들기
   - 테스트 통과가 최우선
   - 완벽하지 않아도 OK
   - "돌아가는 쓰레기"를 만들어도 괜찮다

2. ✨ Make it right (올바르게)
   - 코드를 이해하기 쉽게 정리
   - Tidy First 원칙 적용
   - 리팩토링 및 구조 개선
   - "쓰레기"를 "깨끗한 코드"로 변환

3. 🚀 Make it fast (빠르게)
   - 성능 측정 먼저! (추측하지 말 것)
   - 병목 지점 파악
   - 필요한 곳만 최적화
   - 과도한 최적화 지양 (YAGNI)
```

**금지 사항**:
- ❌ 동작하지 않는데 최적화 시도
- ❌ 동작은 하는데 코드가 엉망인 채로 최적화
- ❌ "나중에 정리하지" 하고 안 하기
- ✅ 반드시 순서대로: Work → Right → Fast

---

## 커밋 원칙

### 커밋 메시지는 반드시 한글로 작성

```bash
# ✅ Good
git commit -m "tidy: 복잡한 조건문을 가드 절로 변경"
git commit -m "feat: 사용자 알림 기능 추가"
git commit -m "fix: null 포인터 예외 수정"
git commit -m "perf: 데이터베이스 쿼리 최적화"

# ❌ Bad
git commit -m "feat: add user notification"  # 영어 사용
git commit -m "refactoring and add feature"  # 정리와 기능 섞음
git commit -m "fix stuff"  # 불명확
```

### 커밋 타입 (한글 메시지)

```bash
tidy:  # 코드 정리 (기능 변경 없음, Tidy First)
feat:  # 새 기능 추가 (Make it work)
fix:   # 버그 수정
test:  # 테스트 추가/수정
perf:  # 성능 최적화 (Make it fast)
docs:  # 문서 업데이트
style: # 코드 포맷팅 (의미 변경 없음)
chore: # 빌드/설정 변경
```

### 커밋 분리 전략

```bash
# 예시: 새 기능 추가 과정

# 1단계: 기존 코드 정리 (Tidy First)
git commit -m "tidy: 사용자 검증 로직을 별도 함수로 추출"
git commit -m "tidy: 매직 넘버를 상수로 변경"

# 2단계: 기능 구현 (Make it work)
git commit -m "feat: 사용자 알림 기능 추가 (초안)"

# 3단계: 코드 정리 (Make it right)
git commit -m "tidy: 알림 전송 로직 단순화"
git commit -m "tidy: 에러 핸들링 개선"

# 4단계: 최적화 (Make it fast, 필요시)
git commit -m "perf: 알림 대량 전송 시 배치 처리 적용"
```

---

## 코드 작성 원칙

### 함수 작성

```typescript
// ✅ Good: 작고, 명확하고, 한 가지 일만
function calculateTotalPrice(items: Item[]): number {
  return items.reduce((sum, item) => sum + item.price, 0);
}

function isValidEmail(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

// ❌ Bad: 길고, 여러 일을 함
function processUserData(data: any) {
  // 100줄의 코드...
  // 검증도 하고, 저장도 하고, 이메일도 보내고...
}
```

**함수 가이드라인**:
- 함수는 **20줄 이하**로 유지
- **한 가지 일만** 수행
- **Guard clauses** 사용하여 중첩 최소화 (최대 3단계)
- **의미있는 이름** (동사 + 명사)
- 주석보다는 **자명한 코드**

### Guard Clauses (조기 리턴)

```typescript
// ✅ Good: Guard clauses
function createUser(data: UserInput) {
  if (!data.email) {
    throw new Error('Email required');
  }
  if (!isValidEmail(data.email)) {
    throw new Error('Invalid email');
  }
  if (data.age < 18) {
    throw new Error('Must be 18+');
  }

  // 실제 로직 (들여쓰기 없이 명확)
  return saveUser(data);
}

// ❌ Bad: 중첩 if
function createUser(data: UserInput) {
  if (data.email) {
    if (isValidEmail(data.email)) {
      if (data.age >= 18) {
        // 실제 로직 (3단계 들여쓰기)
        return saveUser(data);
      }
    }
  }
}
```

### 변수명

```typescript
// ✅ Good: 의미있는 이름
const totalAmount = calculateTotal(transactions);
const isUserActive = checkUserStatus(userId);
const maxRetryCount = 3;

// ❌ Bad: 불명확한 이름
const x = calc(data);
const flag = check(id);
const n = 3;
```

---

## 코딩 시작 전 체크리스트

새 기능을 추가하기 전에 **반드시** 확인:

```
□ 이 코드를 처음 보는 사람이 5분 안에 이해할 수 있는가?
  → NO라면 먼저 정리 (Tidy First)

□ 함수가 20줄 이하인가?
  → NO라면 분리

□ 중첩이 3단계 이하인가?
  → NO라면 Guard clauses 사용

□ 변수/함수 이름이 명확한가?
  → NO라면 개명

□ 테스트 코드가 있는가?
  → NO라면 추가

□ 죽은 코드나 주석 처리된 코드가 있는가?
  → 있다면 제거
```

---

## 리팩토링 타이밍

### 즉시 정리 (< 5분)
- 오타 수정
- 변수명 개선
- 불필요한 주석 제거
- 간단한 포맷팅
- 죽은 코드 제거

### 기능 추가 직전 (10-30분)
- 함수가 너무 길다 → 분리
- 이름이 불명확하다 → 개명
- 중복 코드가 있다 → 통합
- 복잡한 조건문 → Guard clauses

### 나중에 정리 (1시간+)
- 전체 아키텍처 변경
- 데이터 구조 재설계
- 의존성 제거
- **별도 이슈로 등록하고 계획적으로 진행**

---

## 작은 정리 패턴

Kent Beck이 제안하는 패턴들:

1. **Guard Clauses** - 조기 리턴으로 중첩 제거
2. **Extract Helper Function** - 긴 함수를 작은 함수들로 분리
3. **Explaining Variable** - 복잡한 표현식을 의미있는 변수로
4. **Dead Code Removal** - 사용하지 않는 코드 제거
5. **Normalize Symmetries** - 대칭적인 코드 구조 유지
6. **Explaining Comment → Function Name** - 주석을 함수명으로

---

## 테스트

### 테스트 우선순위
1. **동작 검증** - 기능이 제대로 작동하는가?
2. **엣지 케이스** - 경계값, null, 빈 값 등
3. **에러 케이스** - 예외 상황 처리

### 테스트 작성 시기
- ✅ 기능 추가 전 (TDD)
- ✅ 기능 추가 후 즉시
- ✅ 버그 발견 시 재현 테스트 먼저

---

## 성능 최적화 (Make it Fast)

### 최적화 순서
```
1. 측정 (Measure)
   - 추측하지 말고 프로파일링
   - 병목 지점 파악

2. 최적화 (Optimize)
   - 가장 느린 부분만
   - 한 번에 하나씩

3. 재측정 (Measure Again)
   - 실제로 빨라졌는지 확인
   - 회귀 테스트
```

### 최적화 원칙
- ❌ 추측으로 최적화
- ❌ 모든 것을 최적화
- ✅ 측정 → 최적화 → 측정
- ✅ 필요한 곳만 최적화

---

## 우선순위

모든 프로젝트에서 다음 순서를 따름:

1. **정확성** - 올바르게 동작
2. **가독성** - 이해하기 쉬운 코드
3. **유지보수성** - 변경하기 쉬운 구조
4. **성능** - 필요한 경우에만
5. **확장성** - 미래를 고려하되 과도하지 않게

---

## 금지 사항

### 절대 하지 말 것
- ❌ 동작하지 않는 코드 커밋
- ❌ 테스트 없는 기능 추가
- ❌ 의미없는 커밋 메시지
- ❌ 거대한 단일 커밋 (100줄+)
- ❌ 정리와 기능을 섞은 커밋
- ❌ "나중에 정리하지" (하지 않음)
- ❌ 과도한 추상화 (YAGNI)
- ❌ 조기 최적화 (Make it work first!)

---

## 코드 리뷰 자가 점검

커밋 전에 스스로 확인:

```
□ 코드가 이해하기 쉬운가?
□ 테스트가 통과하는가?
□ 커밋이 한 가지 목적만 가지는가?
□ 커밋 메시지가 한글이고 명확한가?
□ 불필요한 변경이 포함되지 않았는가?
□ 문서/주석이 업데이트되었는가?
□ 죽은 코드를 남기지 않았는가?
```

---

## 예외 상황

다음 경우에는 원칙을 유연하게 적용:

1. **프로토타입/POC** - Make it work만
2. **긴급 핫픽스** - 빠른 수정 우선, 정리는 후속 PR
3. **레거시 코드** - 점진적 개선
4. **실험적 기능** - 동작 확인 후 결정

하지만 **반드시 후속 조치**:
```bash
git commit -m "fix: 긴급 버그 수정 (핫픽스)"
# 나중에...
git commit -m "tidy: 핫픽스 코드 정리"
```

---

## 기억할 것

**"코드는 읽히기 위해 작성된다. 실행은 부수 효과일 뿐이다."** - Kent Beck

**"Make it work, make it right, make it fast - In that order."**

**"정리를 먼저, 그 다음 기능"** - Tidy First

**"캠프장을 처음 왔을 때보다 더 깨끗하게"** - Boy Scout Rule

---

## Immutability (CRITICAL)

객체를 절대 직접 수정하지 마세요:

```javascript
// ❌ Wrong
function updateConfig(config, key, value) {
  config[key] = value;
  return config;
}

// ✅ Correct
function updateConfig(config, key, value) {
  return { ...config, [key]: value };
}
```

배열도 동일하게 적용:

```javascript
// ❌ Wrong
items.push(newItem);

// ✅ Correct
const newItems = [...items, newItem];
```

---

## 파일 크기 제한

| 기준 | 줄 수 |
|------|-------|
| **권장** | 200-400줄 |
| **허용** | 400-600줄 |
| **최대** | 800줄 |

초과 시 → 파일 분리 필수

---

## 보안 체크리스트 (커밋 전 필수)

```
□ 하드코딩된 시크릿 없음 (API 키, 비밀번호)
□ 사용자 입력 검증됨
□ 에러 메시지에 민감 정보 노출 없음
□ .env 파일이 .gitignore에 포함됨
□ 민감한 데이터 로깅하지 않음
```

### Secret Management

```javascript
// ❌ Wrong
const API_KEY = "sk-1234567890";

// ✅ Correct
const API_KEY = process.env.API_KEY;
if (!API_KEY) {
  throw new Error("API_KEY environment variable is required");
}
```

---

## 모델 선택 가이드

| 모델 | 용도 | 비용 |
|------|------|------|
| **Haiku 4.5** | 간단한 작업, 빈번한 호출, 형식 변환 | 저렴 |
| **Sonnet 4.5** | 일반 개발 작업 (기본 선택) | 중간 |
| **Opus 4.6** | 복잡한 아키텍처, 대규모 코드베이스, Agent Teams | 높음 |

### 선택 기준

- 반복 작업, 단순 변환 → Haiku
- 코드 작성, 버그 수정 → Sonnet
- 설계 결정, 복잡한 리팩토링, Agent Teams → Opus

---

## 테스트 커버리지

- **목표**: 80% 이상
- **필수**: Unit, Integration 테스트
- **권장**: E2E 테스트 (핵심 플로우)

### 테스트 우선순위

1. 핵심 비즈니스 로직
2. 외부 API 통합 지점
3. 에러 핸들링 경로

---

## Input Validation

Zod 또는 유사 라이브러리 사용 권장:

```typescript
import { z } from 'zod';

const UserSchema = z.object({
  email: z.string().email(),
  age: z.number().min(0).max(150),
  name: z.string().min(1).max(100),
});

function createUser(input: unknown) {
  const validated = UserSchema.parse(input);
  return saveUser(validated);
}
```

---

## 공통 패턴

### API Response Format

```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  meta?: {
    total: number;
    page: number;
    limit: number;
  };
}

function success<T>(data: T): ApiResponse<T> {
  return { success: true, data };
}

function failure(error: string): ApiResponse<never> {
  return { success: false, error };
}
```

### Repository Pattern

```typescript
interface Repository<T> {
  findAll(filters?: Filters): Promise<T[]>;
  findById(id: string): Promise<T | null>;
  create(data: CreateDto): Promise<T>;
  update(id: string, data: UpdateDto): Promise<T>;
  delete(id: string): Promise<void>;
}
```

### Custom Hooks Pattern (React)

```typescript
function useAsync<T>(asyncFn: () => Promise<T>, deps: unknown[]) {
  const [state, setState] = useState<{
    loading: boolean;
    error: Error | null;
    data: T | null;
  }>({
    loading: true,
    error: null,
    data: null,
  });

  useEffect(() => {
    asyncFn()
      .then(data => setState({ loading: false, error: null, data }))
      .catch(error => setState({ loading: false, error, data: null }));
  }, deps);

  return state;
}
```

---

## Code Quality Checklist

커밋 전 자가 점검:

```
□ Immutability 원칙 준수
□ 파일 크기 800줄 미만
□ 함수 크기 20줄 미만
□ 보안 체크리스트 통과
□ 테스트 작성 완료
□ 타입 안전성 확보 (TypeScript)
```

---

## 스킬 특화

### PPTX 스킬

- 빌더 API 사용 권장
- 테마 독립 구조 유지
- `tools/` 폴더는 Python 3.x 필요

### 스킬 구조

```
.claude/skills/<skill-name>/
├── SKILL.md           # 메인 프롬프트
├── lib/               # 라이브러리 코드
├── tools/             # CLI 도구
└── examples/          # 사용 예시
```

---

Last Updated: 2026-01-29

